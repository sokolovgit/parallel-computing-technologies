[project]
name = "lab4-scripts"
version = "0.1.0"
description = "Fetch and layout large text corpora for lab4 ForkJoin benchmarks"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
