# 2 РОЗРОБКА ПОСЛІДОВНОГО АЛГОРИТМУ ТА АНАЛІЗ ЙОГО ШВИДКОДІЇ

Послідовна реалізація є базовим варіантом алгоритму, від якого надалі будується паралельна версія. Тому на цьому етапі важливо не лише отримати коректне сортування, а й зберегти таку структуру обчислень, яка дасть змогу справедливо порівнювати послідовний і паралельний варіанти. Для цього реалізація повинна прямо відповідати псевдокоду алгоритму, працювати для масивів довільної довжини, підтримувати перевірку коректності та надавати можливість вимірювати час виконання на наборах даних різного розміру.

## 2.1 Проєктування послідовної реалізації

Для реалізації було обрано ітеративну форму алгоритму бітонічного сортування. Такий вибір узгоджується з псевдокодом, наведеним у підрозділі 1.2, і дає змогу безпосередньо відобразити шари сортувальної мережі у вкладені цикли програми. Зовнішній цикл відповідає змінюванню розміру блока `k`, а внутрішній - змінюванню кроку `j`, що визначає пари елементів для compare-swap операцій.

Оскільки класичний алгоритм bitonic sort природно визначений для масивів розміру `2^k`, у реалізації передбачено попередню підготовку вхідних даних. Якщо кількість елементів не є степенем двійки, масив доповнюється до найближчої більшої такої довжини службовими значеннями, більшими за максимум вхідного масиву. Після завершення сортування ці додані елементи відкидаються. Такий підхід дає змогу зберегти правильність роботи алгоритму та водночас підтримати обробку вхідних даних довільної довжини.

Для коректного порівняння з майбутньою паралельною реалізацією послідовний алгоритм виконує ті самі базові compare-swap операції над тими самими індексами, що й паралельний. Різниця між реалізаціями надалі полягатиме лише в способі розподілу цих операцій між виконавцями, а не в математичній суті алгоритму. Саме тому послідовна реалізація виступає не тільки як самостійний програмний модуль, а і як еталон для перевірки коректності та вимірювання прискорення.

## 2.2 Програмна реалізація послідовного алгоритму

Послідовна реалізація розміщена в модулі `SequentialBitonicSorter`. Основна логіка сортування винесена в окрему функцію `_bitonic_sort_iterative`, яка виконує сортування in-place над робочим масивом. На рівні класу реалізовано підготовку вхідних даних, запуск сортування, обрізання доповнення та, за потреби, формування результату у спадному порядку.

Базовий клас `BitonicSorter` містить допоміжні методи для обчислення найближчого степеня двійки, доповнення масиву та вимірювання часу виконання. Такий поділ спрощує структуру коду і дає змогу використовувати однакові підготовчі дії як для послідовної, так і для паралельної реалізацій.

У послідовній версії використано `array.array("q")`, тобто компактне зберігання 64-бітних цілих чисел. Це рішення зменшує накладні витрати пам'яті порівняно зі звичайним списком Python і покращує локальність доступу до даних. Додатково в середині внутрішнього циклу використовується локальне посилання на масив, щоб зменшити кількість зайвих звернень до атрибутів і трохи скоротити накладні витрати інтерпретатора.

Логіка compare-swap операції реалізована так: для кожного індексу `i` обчислюється партнер `partner = i ^ j`. Якщо `partner <= i`, пара пропускається, щоб не виконувати одне й те саме порівняння двічі. Далі за допомогою виразу `(i & k) == 0` визначається, чи повинна відповідна підпослідовність на поточному етапі впорядковуватися за зростанням чи за спаданням. Якщо порядок порушено, елементи міняються місцями. Такий підхід добре відображає структуру сортувальної мережі: кожний шар мережі відповідає фіксованому значенню `j`, а повний етап побудови або злиття бітонічної послідовності - фіксованому значенню `k`.

Фрагмент коду 2.1 показує основну частину ітеративної реалізації з вкладеними циклами за `k` та `j`, обчисленням партнера і виконанням compare-swap.

```python
def _bitonic_sort_iterative(arr: array | list[int], n: int) -> None:
    """Sort arr[0:n] in-place (ascending). n must be a power of 2."""
    if n <= 1:
        return

    k = 2
    while k <= n:
        j = k >> 1
        while j >= 1:
            a = arr
            for i in range(n):
                partner = i ^ j
                if partner <= i:
                    continue
                want_asc = (i & k) == 0
                vi, vp = a[i], a[partner]
                if (vi > vp) == want_asc:
                    a[i], a[partner] = vp, vi
            j >>= 1
        k <<= 1
```

Фрагмент коду 2.2 ілюструє підготовку вхідного масиву до сортування, зокрема доповнення до найближчого степеня двійки.

```python
@staticmethod
def _next_power_of_two(n: int) -> int:
    if n <= 0:
        return 1
    p = 1
    while p < n:
        p <<= 1
    return p

def _prepare_padded(self, arr: list[int]) -> tuple[list[int], int]:
    n = len(arr)
    if n == 0:
        return [], 0

    padded_n = self._next_power_of_two(n)
    if padded_n > n:
        sentinel = max(arr) + 1
        padded = list(arr) + [sentinel] * (padded_n - n)
    else:
        padded = list(arr)

    return padded, n
```

## 2.3 Тестування та доведення коректності

Для перевірки коректності було підготовлено набір тестів, що охоплює як прості, так і нетривіальні вхідні дані. Послідовна реалізація перевіряється на порожніх масивах, масивах з одного та двох елементів, уже відсортованих і обернено відсортованих послідовностях, масивах з повторюваними значеннями, від'ємними числами, а також на масивах довжини, що не є степенем двійки.

Окремо перевіряється підтримка спадного порядку сортування, відсутність модифікації вхідного списку, а також коректність роботи на великих випадково згенерованих масивах. Щоб не сповільнювати щоденну перевірку проєкту, тести для дуже великих наборів даних розміру від `524 288` до `1 000 000` елементів винесено в окремий файл і запускаються окремо. Для всіх перевірок очікуваним результатом виступає результат стандартного сортування засобами Python, тобто `sorted(arr)` або `sorted(arr, reverse=True)`.

Такий спосіб тестування є достатньо надійним для цього завдання, оскільки дозволяє перевірити і базові граничні випадки, і роботу реалізації на великих масивах з різними характеристиками вхідних даних. Крім того, саме послідовна реалізація надалі використовується як еталон для перевірки паралельного алгоритму.

Таблиця 2.1 узагальнює основні групи тестів, які використовувалися для перевірки послідовної реалізації.

| № | Група тестів | Приклад вхідних даних | Очікуваний результат | Призначення тесту |
| --- | --- | --- | --- | --- |
| 1 | Порожній масив | `[]` | `[]` | Перевірка коректної обробки граничного випадку без елементів |
| 2 | Один елемент | `[42]` | `[42]` | Перевірка найпростішого нетривіального випадку |
| 3 | Два елементи | `[5, 3]` | `[3, 5]` | Перевірка базової compare-swap операції |
| 4 | Довжина `2^k` | `[10, 30, 11, 20, 4, 330, 21, 110]` | Відсортований масив | Перевірка роботи алгоритму в класичному для bitonic sort випадку |
| 5 | Довжина не `2^k` | `[5, 3, 8, 1, 9]` | `[1, 3, 5, 8, 9]` | Перевірка логіки доповнення до найближчого степеня двійки |
| 6 | Уже відсортований масив | `[0, 1, 2, ..., 15]` | Той самий масив | Перевірка відсутності порушення правильного порядку |
| 7 | Обернений порядок | `[16, 15, ..., 1]` | `[1, 2, ..., 16]` | Перевірка одного з найскладніших для сортування випадків |
| 8 | Масив з дублікатами | `[3, 1, 4, 1, 5, 9, 2, 6, 5, 3, 5]` | Результат `sorted(arr)` | Перевірка коректної роботи за наявності однакових значень |
| 9 | Від'ємні числа | `[-5, 3, -1, 8, 0, -3, 7, 2]` | `[-5, -3, -1, 0, 2, 3, 7, 8]` | Перевірка коректного порівняння елементів різного знака |
| 10 | Великий випадковий масив | 1024 випадкових елементи | Результат `sorted(arr)` | Перевірка коректності на достатньо великому наборі даних у швидкому наборі тестів |
| 11 | Дуже великий масив | `524 288 - 1 000 000` елементів | Результат `sorted(arr)` | Окрема довга перевірка коректності на великих обсягах даних |
| 12 | Сортування за спаданням | `[10, 30, 11, 20, 4, 330, 21, 110]` | `sorted(arr, reverse=True)` | Перевірка додаткового режиму `ascending=False` |
| 13 | Незмінність вхідних даних | `[5, 2, 8, 1]` | Початковий масив не змінюється | Перевірка того, що метод повертає новий результат і не псує вхідний список |

У практичній роботі це дає змогу розділити перевірку на два режими: швидкий набір тестів для повсякденної розробки та окремий повільний запуск для великих масивів. Такий підхід зручний, оскільки основний цикл розробки залишається швидким, але при цьому зберігається можливість підтвердити коректність алгоритму на обсягах даних, близьких до експериментальних. На рисунку 2.1 показано приклад запуску тестів для перевірки коректності послідовної реалізації.

![Рисунок 2.1](media/image4.png)

Рисунок 2.1 -- Запуск тестів для перевірки коректності послідовного алгоритму бітонічного сортування.

## 2.4 Аналіз швидкодії послідовного алгоритму

Дослідження швидкодії виконувалося на платформі `macOS-15.7.3-arm64-arm-64bit Apple M1 Pro` з 8 ядрами продуктивності CPU та Python `3.12.12`. Для серії експериментів використовувалися масиви розмірів від `16` до `4 194 304` елементів. Для кожної конфігурації виконувалося `12` запусків, із яких `2` були прогрівальними, а `10` використовувалися для розрахунку статистики часу виконання. Для підвищення відтворюваності дані генерувалися з фіксованим зерном випадковості.

Отримані результати показують очікуване зростання часу виконання зі збільшенням розміру вхідного масиву. Наприклад, для масиву з `16 384` елементів медіанний час виконання становив приблизно `0,1276` с, для `131 072` елементів - `1,4737` с, для `1 048 576` елементів - `16,1288` с, а для `4 194 304` елементів - `77,8639` с. Така динаміка узгоджується з теоретичною складністю `O(n log^2 n)` і показує, що на великих обсягах даних послідовне виконання стає достатньо повільним.

Із результатів вимірювання важливим є не лише сам факт зростання часу, а й висновок про практичну доцільність паралельної реалізації. Якщо для малих масивів послідовний алгоритм виконується відносно швидко, то для масивів розміру від сотень тисяч до мільйонів елементів час виконання вже вимірюється секундами або десятками секунд. Отже, саме на таких обсягах даних стає доцільним дослідження паралельної реалізації, оскільки накладні витрати на створення процесів і синхронізацію можуть бути компенсовані виграшем у швидкодії. На рисунку 2.2 показано загальну залежність часу виконання від розміру вхідного масиву, а таблиця 2.2 містить характерні числові результати вимірювань.

![Рисунок 2.2](media/image5.png)

Рисунок 2.2 -- Залежність часу виконання послідовного алгоритму бітонічного сортування від розміру вхідного масиву.

| Розмір масиву (n) | Медіанний час (с) | Відхилення (±с) |
| --- | --- | --- |
| 8 192 | 0.0556 | 0.0004 |
| 16 384 | 0.1276 | 0.0005 |
| 32 768 | 0.2894 | 0.0003 |
| 65 536 | 0.6556 | 0.0004 |
| 131 072 | 1.4737 | 0.0011 |
| 262 144 | 3.2860 | 0.0007 |
| 524 288 | 7.2769 | 0.0038 |
| 1 048 576 | 16.1288 | 0.0342 |
| 2 097 152 | 35.6340 | 0.0443 |
| 4 194 304 | 77.8639 | 0.0923 |

Таблиця 2.2 -- Результати вимірювання часу виконання послідовного алгоритму.

## 2.5 Висновки до розділу

У результаті було побудовано послідовну реалізацію алгоритму бітонічного сортування, яка безпосередньо відповідає його ітеративному псевдокоду та коректно працює для масивів довільної довжини за рахунок попереднього доповнення до найближчого степеня двійки. Тестування на граничних випадках, випадкових даних, масивах з повторами та не степеневою довжиною підтвердило коректність реалізації.

Експериментальне дослідження швидкодії показало, що зі зростанням розміру масиву час виконання швидко збільшується і для великих вхідних даних досягає десятків секунд. Це підтверджує доцільність побудови паралельної реалізації алгоритму та подальшого дослідження отриманого прискорення.
