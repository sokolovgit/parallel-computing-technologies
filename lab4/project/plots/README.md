# Lab4 plots

Reads CSV from `just benchmark-task1` / `just benchmark-task1-corpus` (`Task1Main bench` / `bench-corpus`) and writes PNG figures under `results/figures/`.

From `lab4/project`:

```bash
just plot
just plot input=results/task1_corpus_benchmark.csv out=results/figures_corpus
```
