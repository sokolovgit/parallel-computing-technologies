## Learned User Preferences
- For this coursework, treat only external books, articles, publications, documents, and official documentation as bibliography sources; do not count methodology files, current code, tests, or internal project files as literature sources.
- For coursework prose, prefer updating the section-specific files in `course-work/docs/sections/` instead of heavily editing the aggregated `course-work/docs/report/course-work.md`.
- Keep section files clean and final-looking: only the updated text and markdown markup, without draft notes, service blocks, or placeholder instructions.
- For coursework text edits, avoid broad paragraph reflow; prefer minimal, locally clear changes that keep diffs easy to review.
- In the coursework text, every table and figure should be referenced in the surrounding body text; if a figure or table is borrowed, include the source in its caption.
- For coursework diagrams (SVG/draw.io/Mermaid), keep them minimal and report-ready; make parallelization opportunities evident from the diagram structure rather than explicit “can be parallelized” labels.
- Match the visual style to existing diagrams (e.g., `course-work/docs/figures/flow.drawio`): simple boxes/diamonds and orthogonal arrows.
- Prefer ASCII labels in SVG when Cyrillic encoding previously caused garbled text.

## Learned Workspace Facts
- This workspace contains course-work materials and code for the topic `Алгоритм бітонічного сортування та його паралельна реалізація Python`.
