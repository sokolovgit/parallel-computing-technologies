> **Звіт**
>
> з дисципліни «Технології паралельних обчислень» (за змістом курсу)
>
> Комп'ютерний практикум №6
>
> «Розробка паралельного алгоритму множення матриць з використанням MPI-методів обміну повідомленнями „один-до-одного“ та дослідження його ефективності»
>
> **[Виконав:]** …
>
> **[Прийняв:]** …
>
> **Київ — 2026**

### Комп'ютерний практикум №6

Практикум присвячений **розподіленим обчисленням** за моделлю **передачі повідомлень** (стандарт **MPI**), метода **point-to-point** (один-до-одного) у **блокуючому** та **неблокуючому** варіантах, реалізації **паралельного множення матриць** у середовищі MPI та **аналізу ефективності** залежно від розміру задачі й кількості процесів. Теоретична база та терміни узгоджені з лекційними матеріалами (розділи 2.1–2.4 курсу) і зведені в робочому конспекті [`lab6/docs/knowledge-base.md`](../knowledge-base.md). Постановка пунктів 2–4 — у файлі [`lab6/docs/task/lab6.txt`](../task/lab6.txt).

---

### Завдання 1

Потрібно **ознайомитись** із методами **блокуючого** та **неблокуючого** обміну повідомленнями типу **point-to-point** згідно з лекцією та документацією стандарту MPI.

**Блокуючий обмін.** Виклики **`MPI_Send`** та **`MPI_Recv`** призупиняють виконання процесу до моменту, коли операцію можна безпечно вважати завершеною з точки зору семантики MPI: для **`MPI_Recv`** — дані повністю записані в буфер отримувача; для **`MPI_Send`** — дані передані так, що буфер відправника можна знову використовувати (копіювання в системний буфер або безпосередньо до отримувача залежить від реалізації). Це спрощує програмування, але може зменшувати можливість **перекриття** обчислень і комунікації.

**Неблокуючий обмін.** Виклики **`MPI_Isend`**, **`MPI_Irecv`** лише **ініціюють** операцію і повертають **`MPI_Request`**. До виклику **`MPI_Wait`** (або **`MPI_Waitall`** для кількох запитів) **не можна** змінювати буфери, задіяні в операції. Після **`Wait`** гарантується завершення передачі й валідність даних у буфері прийому. Такий стиль дозволяє вставляти корисні обчислення між постановкою обміну в чергу та очікуванням, зменшуючи простої через мережу. **Будь-який** тип надсилання можна поєднувати з **будь-яким** типом прийому (наприклад, блокуюче надсилання з неблокуючим прийомом), якщо дотримано правил узгодження тегів і джерел.

**Додаткові поняття** (лекція 2.2–2.3): **комунікатор** **`MPI_COMM_WORLD`**, **ранг** процесу, **тег** повідомлення, структура **`MPI_Status`** (джерело, тег, код помилки), вимір часу **`MPI_Wtime`** для подальших експериментів. Офіційні специфікації: [mpi-forum.org/docs](https://www.mpi-forum.org/docs/).

**Таблиця 1.1** — Порівняльні риси блокуючого та неблокуючого point-to-point (узагальнення з конспекту та лекцій).

| Аспект | Блокуючий (`Send` / `Recv`) | Неблокуючий (`Isend` / `Irecv` + `Wait`) |
| ------ | ----------------------------- | ----------------------------------------- |
| Повернення з виклику | Після завершення операції в сенсі MPI | Одразу після **постановки** в чергу |
| Буфер після виклику | Для `Recv` — дані готові; для `Send` — буфер відправника вільний для повторного використання | До `Wait` — **не змінювати** відповідні буфери |
| Перекриття з обчисленнями | Обмежене | Можливе між `I*` та `Wait` |
| Складність для програміста | Нижча | Вища (порядок, deadlock, буфери) |

У межах каталогу [`lab6/project`](../../project) підготовлено коротку **демонстраційну програму** для двох MPI-процесів: спочатку передача одного **`MPI_INT`** від рангу $0$ до рангу $1$ блокуючими викликами, потім — неблокуючими з **`MPI_Wait`**. Збірка та запуск описані в `justfile` того каталогу.

**Рисунок 1.1** — Скріншот консолі після успішного запуску демонстрації завдання 1 (за потреби зберегти як `media/lab6_task1_console.png` у `lab6/docs/report/media/`).

**Висновок (завд. 1).** Закріплено різницю між **блокуючими** та **неблокуючими** point-to-point викликами MPI і підготовлено робоче середовище проєкту для подальших завдань (множення матриць та вимірювання ефективності).

---

### Завдання 2

*(Буде доповнено після реалізації паралельного множення матриць з **блокуючими** викликами MPI.)*

---

### Завдання 3

*(Буде доповнено після реалізації варіанту з **неблокуючими** викликами MPI.)*

---

### Завдання 4

*(Буде доповнено після експериментів з ефективністю та порівнянням блокуючого й неблокуючого варіантів.)*

---

### Висновок

У межах початкового етапу практикуму №6 опрацьовано **теоретичні основи** MPI point-to-point (завдання 1), узгоджено їх з лекційними PDF та конспектом у репозиторії. Подальші пункти присвячені **програмуванню** множення матриць і **експериментальному** порівнянню реалізацій.

---

*Примітка для перенесення у Word:* формули в доларах $…$ і блоках $$…$$ зручно конвертувати в DOCX засобом на кшталт Pandoc; рівняння можна відтворити в Word через **Вставка → Рівняння**.
