[project]
name = "lab3-scripts"
version = "0.1.0"
description = "Utilities for lab3 benchmark CSV (hierarchical Excel tables)"
requires-python = ">=3.10"
dependencies = [
  "pandas>=2.0",
  "openpyxl>=3.1",
]
